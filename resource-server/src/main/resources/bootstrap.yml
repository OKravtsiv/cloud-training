spring:
  cloud:
    config:
      name: resource-server
      discovery:
        service-id: configserver
        enabled: true

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka



