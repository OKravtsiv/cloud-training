spring:
  cloud:
    config:
      name: gateway
      discovery:
        service-id: configserver
        enabled: true

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka