spring:
  cloud:
    config:
      name: example-service1
      discovery:
        service-id: configserver
        enabled: true

eureka:
  client:
      serviceUrl:
        defaultZone: http://localhost:8761/eureka/
